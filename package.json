{
  "name": "drupal-admin-ui",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "yarn": ">= 1.9",
    "node": ">= 8.12"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspace admin-ui test"
  }
}
